{
  "name": "hirelens-ats",
  "version": "1.0.0",
  "description": "HireLens ATS — AI-Powered Smart Resume Screener",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
    "vercel-build": "cd frontend && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@vercel/node": "^3.2.27",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.3",
    "multer": "1.4.5-lts.1",
    "openai": "^4.77.3",
    "pdf-parse": "^1.1.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.10.2",
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.7.2"
  }
}
