{
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
