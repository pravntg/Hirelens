{
  "version": 2,
  "buildCommand": "cd frontend 2>/dev/null || true; npm run build; mkdir -p ../dist 2>/dev/null || true; cp -r dist/* ../dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
